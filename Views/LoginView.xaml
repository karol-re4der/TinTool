<Window x:Class="Tintool.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:sys="clr-namespace:System;assembly=System.Runtime"
        xmlns:cal="http://www.caliburnproject.org"
        mc:Ignorable="d"
        Title="Tintool - Login" Height="400" Width="300" Background="#FF2E2E2E" SizeToContent="WidthAndHeight">

    <Window.Resources>
        <sys:String x:Key="TokenInput">Token</sys:String>
    </Window.Resources>

    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Stretch" Margin="0,0,0,0" Grid.ColumnSpan="2" Orientation="Vertical" FlowDirection="LeftToRight">
        <TextBox Name="TokenBox" Text="{Binding Token}" Width="300" Height="35" Margin="10,10,10,10" FlowDirection="LeftToRight" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" VerticalAlignment="Top"/>
        <Button Content="Authenticate" Width="150" Height="50" Margin="10,10,10,10" HorizontalAlignment="Stretch" VerticalAlignment="Bottom" cal:Message.Attach="[Event Click] = [Action LoginAction()]"/>
        </StackPanel>

</Window>
