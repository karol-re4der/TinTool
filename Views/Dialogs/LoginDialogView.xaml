<Window x:Class="Tintool.Views.Dialogs.LoginDialogView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:cal="http://www.caliburnproject.org"
        xmlns:local="clr-namespace:Tintool.Views.Dialogs"
        mc:Ignorable="d"
        Title="Log in" Height="300" Width="200">
    <StackPanel Orientation="Vertical">
        <TextBlock>Phone Number</TextBlock>
        <TextBox Text="{Binding Number}"/>
        <Separator/>
        <TextBlock>Code</TextBlock>
        <TextBox Text="{Binding Code}"/>
        <Separator/>
        <Button cal:Message.Attach="[Event Click]=[LoginButtonClicked()]">Log in</Button>
        <Separator Visibility="Hidden"/>
    </StackPanel>
</Window>
